node_modules/
dist/
.cache/
*.local
.DS_Store

# Índice de cartas gerado no CI. NÃO commitar no main — o workflow o publica
# direto no deploy do Pages. (Veja README > Pipeline de dados.)
public/data/cards.json
